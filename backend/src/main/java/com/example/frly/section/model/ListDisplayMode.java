package com.example.frly.section.model;

public enum ListDisplayMode {
    CHECKBOX,          // Auto-sort: active items first, completed at bottom
    CHECKBOX_STATIC,   // Keep original order, show checkboxes
    ORDERED,           // Numbered list
    UNORDERED          // Bulleted list
}
