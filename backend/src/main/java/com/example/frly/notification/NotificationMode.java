package com.example.frly.notification;

public enum NotificationMode {
    BOTH,           // Both in-app and push notifications
    IN_APP_ONLY,    // Only in-app notifications
    PUSH_ONLY,      // Only push notifications
    NONE            // No notifications
}
